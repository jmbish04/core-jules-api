// src/env.d.ts
// This file defines additional environment types
// The main Env interface is defined in worker-configuration.d.ts

export interface CFEnv extends Env {
    // Additional properties can be added here if needed
    // Currently, CFEnv extends the generated Env interface
  }